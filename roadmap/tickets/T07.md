---
title: "T07 — Domaine : validation réservée au rôle VALIDATOR"
milestone: "Phase 1 — Domaine métier (TDD)"
labels: "phase-1,type:feature"
---
`validate(user)` : seul un utilisateur `VALIDATOR` valide, uniquement une fiche `PENDING_VALIDATION` ; toute autre transition est une erreur explicite. État `VALIDATED` terminal.

Tests attendus : validateur_valideUneFicheEnAttente, operateur_nePeutPasValider, brouillon_nePeutPasEtreValideDirectement, ficheValidee_neSeRevalidePas.
